# Keep repository-wide Cargo behavior portable across developer and CI hosts.
# CPU-specific tuning belongs in explicit release jobs or local RUSTFLAGS,
# not in version-controlled target configuration.
